import React from 'react';
import logo from '../images/logo.svg';

const Navbar: React.FC = () => {
    return (
        <div>
            <img src = {logo} alt='genoshi' />
        </div>
        );
};

export default Navbar;
